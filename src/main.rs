fn main() {
    // Placeholder: the CLI surface (run / diff / undo / commit) is defined in
    // openspec/changes/add-core-sandbox-loop and implemented once approved.
    eprintln!("oops: nothing to undo yet");
}
